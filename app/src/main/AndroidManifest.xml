<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.barran.lib.test">

    <application
        android:name="com.barran.lib.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TestLottieActivity" />
        <activity android:name=".TestViewActivity" />
        <activity android:name="com.barran.lib.thirdlib.TestExcelActivity" />
        <activity android:name="com.barran.lib.thirdlib.TestAdaptiveTableLayoutActivity" />
    </application>

</manifest>
